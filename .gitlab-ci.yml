stages:
  - lint
  - test

image: python:3.11

before_script:
  - pip install -r requirements.txt
  - pip install pytest ruff

lint:
  stage: lint
  script:
    - ruff check . || echo "lint issues (non-blocking)"

test:
  stage: test
  script:
    - pytest -q