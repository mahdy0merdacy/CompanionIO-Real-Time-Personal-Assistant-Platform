stages:
  - test

variables:
  PYTHON_VERSION: "3.11"

test:
  stage: test
  image: python:3.11

  before_script:
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - pip install pytest

  script:
    - pytest

  only:
    - main
    - merge_requests
